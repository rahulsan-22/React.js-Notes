1. What is the difference between var, let and const?

2. What are Template Literals? Mention any two advantages.

3. What is the difference between Explicit Return and Implicit Return in Arrow Functions?

4. What is the difference between Spread Operator and Rest Parameter?

5. What is the difference between map() and forEach()?

6. var a = 10;
   var a = 20;
   console.log(a);

7. let name = "Rahul";
   console.log(`Welcome ${name}`);

8. let num = 11;
   let result = num % 2 === 0 ? "Even" : "Odd";
   console.log(result);

9. console.log(10 && 20);
   console.log(0 && 20);

10. console.log(0 || 100);
    console.log("" || "JavaScript");

11. console.log(null ?? "React");
    console.log("" ?? "JS");

12. let greet = (name = "User") => {
    return name;
    };
    console.log(greet());

13. let arr = [1, 2, 3];
    let output = arr.map((n) => n * n);
    console.log(output);

14. let marks = [35, 45, 95, 55];
    let output = marks.filter((mark) => mark > 50);
    console.log(output);

15. let marks = [35, 45, 95, 55];
    let output = marks.find((mark) => mark > 50);
    console.log(output);

16. Write an Arrow Function to find the cube of a number.

17. Write a program using Ternary Operator to check whether a number is Even or Odd.

18. Using Array Destructuring, extract "HTML" and "JavaScript" from the below array.
    let skills = ["HTML", "CSS", "JavaScript", "React"];

19. Write a function using Rest Parameter that accepts multiple numbers and prints them.

20. Write code using Spread Operator to combine the following arrays into a single array.
    let frontend = ["HTML", "CSS", "JS"];
    let backend = ["NodeJS", "MongoDB"];

21. What is Destructuring?

22. What is Optional Chaining? Explain with an example.

23. What is a Promise? Mention its three states.

24. What is the difference between then/catch and async/await?

25. Explain the complete flow of Fetch API with an example.

26. Explain the difference between filter() and find() with suitable examples.

27. Write all falsy values in JavaScript.

28. What will happen if we try to import a variable that is not exported from a module?

29. Why is map() heavily used in React?

30. Create a small example that uses:

- Arrow Function
- Template Literal
- Default Parameter
  in a single program.
